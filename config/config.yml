# app settings
app:
  name: ${APP_NAME|AuthSSO}

# logger settings
logger:
  level: ${LOG_LEVEL|debug}

# database settings
db:
  postgres:
    dsn: ${POSTGRES_DSN|postgres://user:pass@localhost:5432/authsso?sslmode=disabled}
